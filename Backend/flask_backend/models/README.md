# Model files directory
# Place your trained models here:
#   - xgboost_flood_model.json (XGBoost model)
#   - rl_flood_model.pth (PyTorch RL model)
#
# If models are not present, the system will use
# a statistical heuristic fallback automatically.

